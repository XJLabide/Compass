## Description
Build the `/settings` page: unit system, timezone, protein target, weekly gain target, sign-out. Reads/writes `users/{uid}/profile`.

**Size:** M
**Files:** edit `src/app/(app)/settings/page.tsx`, `src/components/settings/UnitToggle.tsx`, `src/components/settings/TargetInput.tsx`, `src/components/settings/TimezoneSelect.tsx`

## Approach
- Form uses controlled inputs; saves on blur + visible "Saved" status indicator (debounced).
- Timezone: auto-detect via `Intl.DateTimeFormat().resolvedOptions().timeZone`; let user override via a searchable select of IANA zones.
- Unit toggle is display-only — does NOT rewrite history (per gap analyst).
- Sign-out button at the bottom, separated by a divider.

## Acceptance
- [ ] All four target fields persist across reload
- [ ] Unit toggle flips display elsewhere (verify by visiting Dashboard) without changing stored values
- [ ] Timezone change updates `localDate` computation on next check-in
- [ ] Sign-out returns to `/login`

## Done summary
_To be filled in when the task is completed._

## Evidence
_Commands run, outputs, screenshots — added during work._
